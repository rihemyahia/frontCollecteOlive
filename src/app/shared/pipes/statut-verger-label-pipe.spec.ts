import { StatutVergerLabelPipe } from './statut-verger-label-pipe';

describe('StatutVergerLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new StatutVergerLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
