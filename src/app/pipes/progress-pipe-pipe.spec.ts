import { ProgressPipePipe } from './progress-pipe-pipe';

describe('ProgressPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProgressPipePipe();
    expect(pipe).toBeTruthy();
  });
});
