import { StatutTourneePipe } from './statut-tournee-pipe';

describe('StatutTourneePipe', () => {
  it('create an instance', () => {
    const pipe = new StatutTourneePipe();
    expect(pipe).toBeTruthy();
  });
});
