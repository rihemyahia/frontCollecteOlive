import { StatutCollectePipePipe } from './statut-collecte-pipe-pipe';

describe('StatutCollectePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatutCollectePipePipe();
    expect(pipe).toBeTruthy();
  });
});
